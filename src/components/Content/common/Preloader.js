import React from 'react';
import loading from '../../../assets/images/loading.svg';

let Loading = (props) => {
return (
     <div> <img src={loading} style={{display:'block'}} style={{marginLeft:'500px'}}  /> </div>)
    
}
export default Loading;